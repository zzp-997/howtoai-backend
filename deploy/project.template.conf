# ============================================
# 项目部署配置模板
# 复制此文件并修改以下变量
# ============================================

# 项目基本信息
$PROJECT_NAME = "myapp"           # 项目名称（用于目录、容器命名）
$FRONTEND_DOMAIN = "app.example.com"   # 前端域名
$BACKEND_DOMAIN = "api.example.com"    # 后端域名

# 数据库配置
$DB_NAME = "myapp"                # 数据库名
$DB_USER = "myapp"                # 数据库用户
$DB_PASSWORD = "YourPassword123"  # 数据库密码

# JWT密钥（可用 openssl rand -hex 32 生成）
$SECRET_KEY = "your-secret-key-here"

# 服务器配置
$SERVER_IP = "118.25.182.15"
$SERVER_USER = "root"
$REMOTE_DIR = "/opt/$PROJECT_NAME"

# 本地路径
$BACKEND_DIR = "D:\coderzzp\$PROJECT_NAME-backend"
$FRONTEND_DIR = "D:\coderzzp\$PROJECT_NAME"